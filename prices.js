/* Файл prices.js — все цены/константы вынесены сюда для удобного редактирования.
   Пока структура заглушечная: в проекте конкретные цены будут использоваться позже.
   Не изменяйте формат, если будете подключать в код.
*/
const PRICES = {
  currency: 'RUB',
  // Пример: тарифы по позициям (в месяц)
  positionSalary: {
    'СЭ': 50000,
    'АЗ': 42000
  },
  // Пример: процент комиссии или бонусов
  commissions: {
    salesBonusPercent: 3.5
  },
  // Пример: фиксированные надбавки
  allowances: {
    nightShift: 1500,
    remoteWork: 800
  },
  // Дальше добавляйте по необходимости
};
if(typeof module !== 'undefined'){ module.exports = PRICES; }
